# isa: the instruction set as data

`isa.yaml` is the single source of truth. The ISS, the assembler and the RTL decoder are generated from it, so the golden model and the silicon cannot disagree about an encoding.
