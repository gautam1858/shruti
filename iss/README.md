# iss: cycle-accurate instruction-set simulator

The golden model. Written before any RTL, generated from `isa/isa.yaml`, and the reference for differential testing and for `shruti prove`. Runs Event Machine programs against scripted or random pin stimulus and emits the pin trace with cycle timestamps.
