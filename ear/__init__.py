"""The Ear: protocol simulator, bit-exact reference of the feature extractor and ternary
classifier, and the training pipeline. Modules import their own dependencies; `protosim`
and `inpath` are pure Python so the cocotb tests can use them."""
