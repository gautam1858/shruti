# ear: the on-die protocol identifier

Bit-exact fixed-point reference of the feature extractor and the ternary MLP, the protocol simulator that generates training edge streams, the PyTorch training pipeline, the weight export, and the `shruti teach / train / load` CLI.
